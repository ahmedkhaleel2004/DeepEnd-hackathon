import React from "react";

interface Props {
  uid: string;
  projectIndex: number;
}

const Timeline = ({ uid, projectIndex }: Props) => {
  return <div>Timeline</div>;
};

export default Timeline;
